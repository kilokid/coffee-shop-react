import HeaderContainer from '../container/header-container';

import './header.scss';

const Header = () => {
    return (
        <header className="header">
            <HeaderContainer />
        </header>
    );
}

export default Header;